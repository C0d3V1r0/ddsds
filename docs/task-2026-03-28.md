# Nullius Tasks — 28.03.2026

## 1. Structured Detection Framework

Цель:
- оформить detection layer как нормальный внутренний framework, а не набор разрозненных правил

Что сделать:
- единый формат detector result
- явная структура detector pipeline
- registry/каталог детекторов без overengineering
- связка `detection -> enrichment -> policy -> incident`

Готово, если:
- новые детекторы добавляются предсказуемо
- тип события не может появиться в UI без реального detector-а
- есть тесты на базовый pipeline

## 2. Port Scan Detection

Цель:
- добавить реальное распознавание сканирования портов

Что сделать:
- определить источник данных для port scan detection
- реализовать rule-based detector
- завести событие `port_scan` end-to-end
- связать с `Security`, incidents и response policy

Готово, если:
- `nmap` или аналогичный сценарий реально детектится
- событие видно в `Безопасность`
- есть тесты

## 3. ML / Dataset Pipeline Hardening

Цель:
- сделать ML ещё умнее и надёжнее в production-сценариях

Что сделать:
- усилить dataset quality gates
- развить baseline/dataset pipeline
- улучшить связь ML с detection layer
- уменьшить риск обучения на плохих данных

Готово, если:
- pipeline обучения остаётся explainable
- качество dataset видно и в коде, и в UI
- есть тесты на новые правила подготовки данных

## 4. Detection + ML Integration

Цель:
- связать rule-based detection и ML в один более зрелый слой

Что сделать:
- выровнять event model
- сделать более явную интеграцию explainable detections, incidents и response policy
- убрать разрыв между распознаванием, объяснением и реакцией

Готово, если:
- detection и ML работают как единый слой
- нет “висячих” типов событий без рабочего распознавания
- поведение системы читается последовательно
