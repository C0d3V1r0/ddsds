# Nullius Tasks — 29.03.2026

## Цель дня

29 марта — это день завершения итерации.

В этот день не начинаем большие новые ветки.
Фокус только на том, чтобы:

- закончить фичи и фиксы, начатые 28.03
- сделать `Better Logs Investigation`
- провести полный рефакторинг проделанного
- оптимизировать узкие места
- прогнать полный набор проверок
- исправить найденные дефекты
- обновить документацию
- зафиксировать результат как завершённый этап

## Дневной блок

## 1. Better Logs Investigation

Цель:
- сделать расследование событий быстрее и полезнее

Что сделать:
- переход от security event к связанным логам
- фильтрация по IP, времени и типу события
- подсветка релевантных строк и подозрительных фрагментов
- сделать это как реальную продуктовую задачу дня, а не вечерний хвост

Готово, если:
- из события можно быстро перейти к связанным логам
- расследование требует меньше ручного поиска
- поведение проверяемо руками или тестами

Статус:
- выполнено: переход `Security -> Related logs`
- выполнено: фильтрация логов по `IP / query / event_type`
- выполнено: подсветка релевантных строк и подозрительных фрагментов

## Вечерний блок

## 2. Полный рефакторинг проделанного

Цель:
- пройтись по коду после задач 28.03 и дневной задачи 29.03 и привести его к единому качественному виду

Что сделать:
- дочистить detection layer после внедрения framework-подхода
- дочистить ML/dataset pipeline после новых изменений
- выровнять код по `paradigms.md`
- убрать случайную дублирующуюся логику, временные костыли и шероховатости
- усилить комментарии там, где код стал сложнее

Готово, если:
- код читается последовательно
- нет висячих полуархитектурных решений
- новые слои не конфликтуют со старой логикой

Статус:
- выполнено: дополнительный `functional-first` refactor pass по `server/web`
- выполнено: detection и ML pipeline выровнены по `paradigms.md`
- выполнено: случайная дублирующаяся логика в hooks/API дочищена

## 3. Оптимизация и hardening

Цель:
- убрать лишнюю сложность и довести новые фичи до более зрелого состояния

Что сделать:
- проверить detection/ML pipeline на лишние запросы и лишние проходы
- проверить performance узкие места в backend
- проверить, что новые правила и quality gates не создают лишнего шума
- усилить устойчивость runtime там, где это потребуется после реального теста

Готово, если:
- новые функции не выглядят тяжёлыми или хрупкими
- поведение осталось предсказуемым

Статус:
- выполнено: investigation flow переведён на серверную фильтрацию, а не на локальные костыли
- выполнено: project-level cleanup после прогонов и сборок
- выполнено: deploy-скрипты дополнительно проверены через `bash -n`

## 4. Полное тестирование и фиксы

Цель:
- провести полный контроль качества после завершения итерации

Что сделать:
- прогнать server tests
- прогнать agent tests
- прогнать frontend build
- прогнать smoke / e2e / runtime acceptance, если стенд доступен
- руками проверить ключевые сценарии UI
- исправить всё, что всплывёт

Готово, если:
- тестовый контур зелёный
- найденные регрессии исправлены
- нет известных свежих поломок после задач 28.03

Статус:
- выполнено: `pytest`, `go test`, `npm run build`
- выполнено: `playwright --list`
- выполнено: найденная регрессия в `ws/agent` исправлена

## 5. Финальный polish и документация

Цель:
- оформить итерацию как законченный, чистый результат

Что сделать:
- обновить docs под реальное состояние detection/ML слоя
- обновить roadmap / audit / release notes при необходимости
- подчистить формулировки в UI, если после тестирования останутся шероховатости
- удалить оставшийся мусор и временные артефакты

Готово, если:
- документация соответствует коду
- UI и docs говорят об одном и том же
- в репозитории не осталось случайного мусора

Статус:
- выполнено: `README`, `testing/README`, `technical-audit`, сам задачник
- выполнено: проектный мусор после тестов и сборок удалён

## 6. Завершение этапа

Цель:
- поставить явную точку в этой итерации, а не оставлять “почти закончили”

Что сделать:
- зафиксировать, что задачи 28.03 завершены
- отдельно отметить, что реально готово
- отдельно отметить, что осознанно уходит в следующую итерацию

Готово, если:
- можно честно сказать, что этап закончен
- следующие задачи начинаются уже как новый этап, а не как бесконечный хвост старого
