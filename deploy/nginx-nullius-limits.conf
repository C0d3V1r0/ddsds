# Rate limiting для API и WebSocket выносим в http-контекст nginx.
# Так конфиг можно безопасно подключать через conf.d без ручной правки nginx.conf.
limit_req_zone $binary_remote_addr zone=nullius_api:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=nullius_ws:10m rate=5r/s;
